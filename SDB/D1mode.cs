using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SymbolDB
{
    public enum D1function
    {
        DISPLAY1, SLIDESHOW, ANNOTATE, CATALOG, FILE
    }
    public enum PictureLocation
    {
        LEFT, CENTER, STORED, NONE
    }
    public enum PictureSize
    {
        BESTFIT, ACTUAL, ZOOM, CUSTOM
    }
}
